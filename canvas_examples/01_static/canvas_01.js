var canvas = document.getElementById('canvas'); // gets canvas element
var context = canvas.getContext('2d'); // gets canvas' context
context.fillStyle = 'blue'; // sets fill color
context.fillRect(100, 200, 150, 50); // fills a rectangle on x (100), y (200) having width (150) and height (50)